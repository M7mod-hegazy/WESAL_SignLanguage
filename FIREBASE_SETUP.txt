🔥 FIREBASE AUTHENTICATION SETUP INSTRUCTIONS
===============================================

✅ BACKEND IS READY! Now follow these steps:

STEP 1: CREATE FIREBASE PROJECT (5 minutes)
--------------------------------------------
1. Go to: https://console.firebase.google.com/
2. Click "Add project"
3. Name: "SignLanguage" (or any name)
4. Disable Google Analytics (optional)
5. Click "Create project"


STEP 2: ENABLE AUTHENTICATION
------------------------------
1. In Firebase Console, click "Authentication"
2. Click "Get started"
3. Go to "Sign-in method" tab
4. Enable these providers:

   ✅ Email/Password:
      - Click on it
      - Toggle "Enable"
      - Click "Save"

   ✅ Google:
      - Click on it
      - Toggle "Enable"
      - Click "Save"

   ✅ Facebook (Optional - need Facebook App):
      - Click on it
      - Toggle "Enable"
      - Enter App ID and App Secret (from Facebook Developers)
      - Click "Save"

   ✅ Twitter (Optional - need Twitter App):
      - Click on it
      - Toggle "Enable"
      - Enter API Key and API Secret (from Twitter Developer Portal)
      - Click "Save"


STEP 3: GET FIREBASE CONFIG
----------------------------
1. Click the gear icon ⚙️ (Project Settings)
2. Scroll down to "Your apps"
3. Click the web icon </> (Add app)
4. Register app name: "SignLanguage Web"
5. Click "Register app"
6. COPY the firebaseConfig object (looks like this):

   const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "your-project.firebaseapp.com",
     projectId: "your-project-id",
     storageBucket: "your-project.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abc123"
   };


STEP 4: UPDATE FRONTEND CONFIG
-------------------------------
1. Open: frontend/src/config/firebase.js
2. Replace the firebaseConfig object with YOUR config from Step 3
3. Save the file


STEP 5: ADD AUTHORIZED DOMAINS
-------------------------------
1. In Firebase Console → Authentication → Settings
2. Go to "Authorized domains" tab
3. Add: localhost (should already be there)
4. For production, add your domain later


STEP 6: GET FIREBASE ADMIN SDK (FOR BACKEND)
---------------------------------------------
1. In Firebase Console → Project Settings
2. Go to "Service accounts" tab
3. Click "Generate new private key"
4. Click "Generate key" (downloads a JSON file)
5. Save this file as: backend/firebase-admin-key.json
6. IMPORTANT: Add to .gitignore (already done)


STEP 7: UPDATE BACKEND .ENV
----------------------------
Add this line to backend/.env:

FIREBASE_ADMIN_KEY_PATH=./firebase-admin-key.json


STEP 8: TEST THE SETUP
-----------------------
1. Start backend: cd backend && npm start
2. Start frontend: cd frontend && npm start
3. Go to: http://localhost:3000
4. Try to register/login


📋 BACKEND API ENDPOINTS (READY TO USE)
========================================

Authentication:
- POST /api/auth/verify - Register/Login user
- GET /api/auth/me - Get current user
- PUT /api/auth/profile - Update profile
- DELETE /api/auth/account - Delete account
- GET /api/auth/stats - Get user stats

Signs (Protected):
- GET /api/signs/random_quiz
- POST /api/signs/:id/check_answer

Progress (Protected):
- GET /api/progress/my_progress
- POST /api/progress/add_coins


🎨 FRONTEND FILES CREATED
==========================
✅ frontend/src/config/firebase.js - Firebase config
✅ frontend/src/services/authService.js - Auth functions


📦 PACKAGES INSTALLED
======================
Backend:
✅ firebase-admin - Firebase Admin SDK
✅ express-validator - Input validation

Frontend:
✅ firebase - Firebase SDK
✅ axios - HTTP client


🔒 SECURITY NOTES
==================
1. ✅ firebase-admin-key.json is in .gitignore
2. ✅ Never commit Firebase config with real keys to public repos
3. ✅ Use environment variables for production
4. ✅ Enable App Check in Firebase for production


🚀 NEXT STEPS
==============
1. Complete Steps 1-7 above
2. Create Login/Register UI components
3. Add social login buttons
4. Protect routes that need authentication
5. Test all auth flows


Need help? Check Firebase docs:
https://firebase.google.com/docs/auth/web/start
